{
  "name": "enceladus",
  "version": "1.0.0",
  "description": "It's my website",
  "dependencies": {
    "@types/three": "^0.131.0",
    "esbuild": "^0.12.20",
    "three": "^0.131.3"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dist": "esbuild ui/index.ts --bundle --minify --sourcemap --outfile=dist/bundle.js",
    "build": "esbuild ui/index.ts --bundle --outfile=dist/bundle.js"
  },
  "author": "",
  "license": "ISC"
}
